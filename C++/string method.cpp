#include <iostream>

int main()
{

    // Std :: string name;
    // Std :: getline(std :: cin , name);

    // Name.length();  == length
    // Name.empty();==  may be don’t enter
    // Name.clear(); ==  don’t enter
    // Name.append("***"); == will add to the string
    // Name.at(1); ==  show the letter at that point .
    // Name.insert(position , "letter") == will add the letter in that position
    // Name.find('*') ==  will give the index of *
    // Name.erase (0 , 3)== will erase  the letter at that position

    return 0;
}